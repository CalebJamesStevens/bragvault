'use client'

import { AppBar, Toolbar, Typography } from "@mui/material";


export default () => {

    return (
        <AppBar position="sticky">
            <Toolbar>
                <Typography variant="body1">Brag Vault</Typography>
            </Toolbar>
        </AppBar>
    )
}