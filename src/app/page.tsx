'use client'
import { Typography } from "@mui/material";

export default function Home() {
  return (
    <main>
      <Typography variant="h1">Brag Vault</Typography>
      <Typography variant="h2">Let your achievements speak for you</Typography>
      <Typography variant="body1">Transforming your achievements into a powerful brag document and unlocking job opportunities like never before.</Typography>
    </main>
  )
}
